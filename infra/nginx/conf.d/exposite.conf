server {
    listen 80;
    server_name exposite.com.br www.exposite.com.br 144.91.70.44;

    # Landing page — export estático do Next.js
    root /var/www/landing;
    index index.html;

    location / {
        try_files $uri $uri/index.html $uri.html /index.html;
    }

    # API Laravel e health check — FastCGI para PHP-FPM
    location ~ ^/(api|up)(/.*)?$ {
        resolver 127.0.0.11 valid=30s ipv6=off;
        set $backend backend:9000;
        root /var/www/backend/public;
        fastcgi_pass $backend;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME /var/www/backend/public/index.php;
        include fastcgi_params;
    }

    location ~ /\.ht {
        deny all;
    }
}

# ──────────────────────────────────────────────────────────────────────────────
# Painel do cliente — app.exposite.com.br
# ──────────────────────────────────────────────────────────────────────────────
server {
    listen 80;
    server_name app.exposite.com.br;

    location / {
        resolver 127.0.0.11 valid=30s ipv6=off;
        set $painel painel:3000;
        proxy_pass http://$painel;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# ──────────────────────────────────────────────────────────────────────────────
# Sites dos clientes — subdomínio wildcard
# Descomente após:
#   1. Configurar DNS: tipo A  *  → 144.91.70.44
#   2. Gerar certificado wildcard:
#      certbot certonly --manual --preferred-challenges dns \
#        -d "exposite.com.br" -d "*.exposite.com.br"
# ──────────────────────────────────────────────────────────────────────────────
#
# server {
#     listen 80;
#     server_name ~^(?<slug>[^.]+)\.exposite\.com\.br$;
#     return 301 https://$host$request_uri;
# }
#
# server {
#     listen 443 ssl;
#     server_name ~^(?<slug>[^.]+)\.exposite\.com\.br$;
#
#     ssl_certificate     /etc/letsencrypt/live/exposite.com.br/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/exposite.com.br/privkey.pem;
#
#     root /var/www/sites/$slug;
#     index index.html;
#
#     location / {
#         try_files $uri $uri/index.html $uri.html /index.html;
#     }
#
#     location ~ /\.ht {
#         deny all;
#     }
# }
